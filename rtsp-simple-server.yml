paths:
  fhd:
    source: rpiCamera
    sourceOnDemand: yes
    sourceOnDemandCloseAfter: 1s
    runOnReady: 'curl -sX POST http://localhost/internal/video-uses-camera'
    runOnReadyRestart: yes

externalAuthenticationURL: 'http://localhost/internal/video-auth'
rtspAddress: :554
api: true
